# =============================================================================
# REFERÊNCIA: Dockerfile para imagem customizada do Airflow
# =============================================================================
# Use este arquivo como base. Renomeie para Dockerfile e adicione as
# dependências necessárias (pip install, drivers, etc.).
# =============================================================================

# FROM apache/airflow:2.7.0

# USER root
# RUN apt-get update && apt-get install -y --no-install-recommends \
#     # adicione pacotes de sistema se precisar (ex.: openjdk para Spark)
#     && apt-get clean \
#     && rm -rf /var/lib/apt/lists/*

# USER airflow
# RUN pip install --no-cache-dir \
#     apache-airflow-providers-http \
#     pyspark
#     # adicione outros providers ou libs

# COPY --chown=airflow:airflow . .
